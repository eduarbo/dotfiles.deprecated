#!/usr/bin/env zsh
source ${0:A:h}/../../deploy

# Dependencies

common_packages=(
  the_silver_searcher
  ripgrep
  sqlite
)

dnf_packages=(
  make
  emacs
)

install() {
  case $(_os) in
    macos)
      brew install ${common_packages[@]}
      brew install emacs --with-cocoa --with-gnutls --with-rsvg --with-imagemagick@6 --devel
      ;;
    fedora)
      sudo dnf install -y ${common_packages[@]} ${fedora_packages[@]}
      ;;
  esac

  # Clone and setup my doom-emacs fork
  _ensure_repo eduarbo/doom-emacs ~/.emacs.d && \
    cd ~/.emacs.d && \
    git checkout develop && \
    mklink init.el ~/.emacs.d/ && \
    YES=1 make install

  # Clone Cask, Project management tool for Emacs
  _ensure_repo cask/cask ~/.emacs.d/cask
}

update() {
  cd ~/.emacs.d && YES=1 make update
  cd ~/.emacs.d/cask && git pull
}

link() {
  mklink doom $XDG_CONFIG_HOME/
}

clean() {
  rm -rfv "$XDG_CONFIG_HOME/doom" "~/.emacs.d"
}

init "$@"
